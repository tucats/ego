{
    "description": "INSERT OR FAIL has no PostgreSQL equivalent -- a failed statement inside a PostgreSQL transaction always poisons the whole transaction, with no per-statement partial-success option -- Rewrite must reject this rather than emit misleading SQL",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-{{DLID}}/tables/@sql",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        },
        "body": [
            "INSERT OR FAIL INTO tF{{DLID}} (id, name) VALUES (1, 'x')"
        ]
    },
    "response": {
        "status": 400,
        "headers": {
            "Content-Type": ["error+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "error message explains the dialect translation failure",
            "query": "msg",
            "op": "contains",
            "value": "cannot translate"
        }
    ]
}
