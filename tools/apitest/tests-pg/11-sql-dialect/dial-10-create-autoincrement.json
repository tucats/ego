{
    "description": "sqlite3's INTEGER PRIMARY KEY AUTOINCREMENT rewritten as PostgreSQL's GENERATED BY DEFAULT AS IDENTITY -- create the table here; dial-11 inserts and verifies the generated ids in a separate call, so the unique-key catalog lookup Rewrite needs for later tests always sees a committed table",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-{{DLID}}/tables/@sql",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        },
        "body": [
            "CREATE TABLE tA{{DLID}} (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT)"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rowcount+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ]
}
