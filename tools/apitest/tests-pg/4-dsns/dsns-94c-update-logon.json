{
    "description": "log on as the non-admin test user to get a bearer token for the UpdateDSNHandler permission tests below",
    "request": {
        "method": "POST",
        "endpoint": "/services/admin/logon",
        "body": {
            "username": "udu{{DSNUUID}}",
            "password": "zork"
        },
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["logon+json"]
        },
        "save": { "UPDATEDSN_TOKEN": "token" }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ]
}
