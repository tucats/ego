{
    "description": "confirm the OAuth2 Authorization Server instance is running",
    "request": {
        "method": "GET",
        "endpoint": "{{AS_URL}}/services/up",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["application/json"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["application/json"]
        },
        "save": { "AS_SERVER_ID": "server.id" }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        }
    ],
    "abort": true
}
