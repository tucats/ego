{
    "description": "confirm ego.runtime.rest.timeout was restored to its original value",
    "request": {
        "method": "POST",
        "endpoint": "/admin/config",
        "headers": {
            "Content-Type": ["application/vnd.ego.config.list+json"],
            "Accept": ["application/vnd.ego.config+json"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        },
        "body": [
            "ego.runtime.rest.timeout"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["application/vnd.ego.config+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "ego.runtime.rest.timeout restored",
            "query": "items.ego\\.runtime\\.rest\\.timeout.value",
            "value": "{{REST_TIMEOUT_ORIG}}"
        }
    ]
}
