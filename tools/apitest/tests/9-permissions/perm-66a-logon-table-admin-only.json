{
    "description": "log on as the table-admin-only test user",
    "request": {
        "method": "POST",
        "endpoint": "/services/admin/logon",
        "body": {
            "username": "tabadmintest",
            "password": "zork"
        },
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["logon+json"]
        },
        "save": { "TABADMIN_TOKEN": "token" }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ]
}
