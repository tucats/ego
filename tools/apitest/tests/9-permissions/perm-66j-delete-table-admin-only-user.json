{
    "description": "clean up the table-admin-only test user itself. Its table_perms grant on t{{PERMTESTID}} was already explicitly revoked at perm-66i (needed before this point so perm-83's exact-count assertion isn't thrown off); any remaining table_perms row for it would in any case be removed once dsn-{{PERMTESTID}} itself is deleted at perm-91, via DeletePermissionsByDSN.",
    "request": {
        "method": "DELETE",
        "endpoint": "/admin/users/tabadmintest",
        "headers": {
            "Accept": ["application/vnd.ego.user+json"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["application/vnd.ego.user+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "user name",
            "query": "name",
            "value": "tabadmintest"
        }
    ]
}
