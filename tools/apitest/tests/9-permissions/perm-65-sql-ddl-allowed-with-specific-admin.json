{
    "description": "REGRESSION for DATA-SECURITY.md §3.8: sqlspecificadmintest now has a DSN-specific admin record (plus read/write to get past DSN-open) but still no identity-level ego.dsn.admin. authorizeStatement's UsageAdmin branch used to check hasPermission(session, defs.DSNAdminPermission) alone -- identity only; it must now also accept AuthDSN(..., DSNAdminAction) for the DSN the CREATE TABLE targets.",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-sq{{PERMTESTID}}/tables/@sql",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{SQLSPECIFICADMIN_TOKEN}}"]
        },
        "body": [
            "CREATE TABLE tsq{{PERMTESTID}} (id INTEGER NOT NULL, name TEXT NOT NULL)"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rowcount+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ]
}
