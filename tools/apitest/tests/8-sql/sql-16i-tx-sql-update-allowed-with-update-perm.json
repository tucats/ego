{
    "description": "REGRESSION for DATA-SECURITY-2.md finding #5, the @transaction side: now that sqtx{{SQLTESTID}} holds ego.table.update specifically (sql-16h), the identical UPDATE-via-'sql'-opcode request sql-16g just denied now succeeds. Still matches no row (same WHERE id = 424242 as sql-16g) -- this test is about the authorization outcome, not the row count.",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-{{SQLTESTID}}/tables/@transaction",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{SQLTX_TOKEN}}"]
        },
        "body": [
            {
                "operation": "sql",
                "sql": "UPDATE t{{SQLTESTID}} SET name = 'ShouldNotApply' WHERE id = 424242"
            }
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rowcount+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "no matching rows updated",
            "query": "count",
            "value": "0"
        }
    ]
}
