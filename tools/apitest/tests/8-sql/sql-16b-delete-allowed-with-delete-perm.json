{
    "description": "REGRESSION for DATA-SECURITY-2.md finding #5: now that squ{{SQLTESTID}} holds ego.table.delete specifically (sql-16a), the identical DELETE statement sql-16 just denied now succeeds. Row id=1 is still present -- sql-16's denial means it was never actually removed -- so this affects exactly the one row sql-16 originally expected.",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-{{SQLTESTID}}/tables/@sql",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{SQL_TOKEN}}"]
        },
        "body": [
            "DELETE FROM t{{SQLTESTID}} WHERE id = 1"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rowcount+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "affected rows count",
            "query": "count",
            "value": "1"
        }
    ]
}
