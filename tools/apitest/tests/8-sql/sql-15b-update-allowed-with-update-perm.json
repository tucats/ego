{
    "description": "REGRESSION for DATA-SECURITY-2.md finding #5: now that squ{{SQLTESTID}} holds ego.table.update specifically (sql-15a), the identical UPDATE statement sql-15 just denied now succeeds -- proving the fix grants exactly the permission that matches the statement, not that it blocks UPDATE outright.",
    "request": {
        "method": "POST",
        "endpoint": "/dsns/dsn-{{SQLTESTID}}/tables/@sql",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{SQL_TOKEN}}"]
        },
        "body": [
            "UPDATE t{{SQLTESTID}} SET name = 'Alicia' WHERE id = 1"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rowcount+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "affected rows count",
            "query": "count",
            "value": "1"
        }
    ]
}
