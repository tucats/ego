{
    "description": "log on as the @transaction test user",
    "request": {
        "method": "POST",
        "endpoint": "/services/admin/logon",
        "body": {
            "username": "sqtx{{SQLTESTID}}",
            "password": "zork"
        },
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["logon+json"]
        },
        "save": { "SQLTX_TOKEN": "token" }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ]
}
