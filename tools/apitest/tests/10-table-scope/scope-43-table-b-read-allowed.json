{
    "description": "REGRESSION: with a table_perms read grant specific to tblB{{TSID}}, the same GET that scope-40 correctly denied must now succeed -- independent of, and without needing, any grant on tblA{{TSID}}",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/dsn-ts{{TSID}}/tables/tblB{{TSID}}/rows",
        "headers": {
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{TS_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rows+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "no rows yet",
            "query": "count",
            "value": "0"
        }
    ]
}
