{
    "description": "REGRESSION for the table.admin-implies-read/write/update/delete fix: user holds ONLY ego.table.admin on tblC{{TSID}} (scope-71), no explicit ego.table.read grant. GET rows (ReadRows, non-abstract) must still succeed -- Authorized()'s TableReadPermission case now accepts perm.Admin in place of perm.Read.",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/dsn-ts{{TSID}}/tables/tblC{{TSID}}/rows",
        "headers": {
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{TS_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rows+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "no rows yet",
            "query": "count",
            "value": "0"
        }
    ]
}
