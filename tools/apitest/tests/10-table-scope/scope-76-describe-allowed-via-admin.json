{
    "description": "REGRESSION for the table.admin-implies-read fix, describe.go path: GET .../tables/{table} (metadata, not /rows) also gates on defs.TableReadPermission via Authorized(). Same user, only ego.table.admin on tblC{{TSID}} (scope-71), no explicit read grant -- must still succeed.",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/dsn-ts{{TSID}}/tables/tblC{{TSID}}",
        "headers": {
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{TS_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["columns+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "column count: 2",
            "query": "count",
            "value": "2"
        }
    ]
}
