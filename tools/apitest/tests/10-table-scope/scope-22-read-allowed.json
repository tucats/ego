{
    "description": "REGRESSION for fdf0872a: same non-abstract GET as scope-20, now that a genuine table_perms read grant exists on tblA{{TSID}} -- must succeed via the plain ReadRows path (not the abstract one)",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/dsn-ts{{TSID}}/tables/tblA{{TSID}}/rows",
        "headers": {
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{TS_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["rows+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        },
        {
            "name": "no rows yet",
            "query": "count",
            "value": "0"
        }
    ]
}
