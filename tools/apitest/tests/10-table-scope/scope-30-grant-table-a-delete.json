{
    "description": "add table_perms ego.table.delete on tblA{{TSID}}",
    "request": {
        "method": "PUT",
        "endpoint": "/dsns/dsn-ts{{TSID}}/tables/tblA{{TSID}}/permissions",
        "parameters": {
            "user": "tsu{{TSID}}"
        },
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        },
        "body": [
            "ego.table.delete"
        ]
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["application/json"]
        }
    },
    "tests": [
        {
            "name": "delete permission granted",
            "query": "permissions.*",
            "value": "ego.table.delete"
        }
    ]
}
