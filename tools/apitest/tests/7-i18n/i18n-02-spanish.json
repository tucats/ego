{
    "description": "GET a nonexistent DSN with Accept-Language: es. The server should negotiate Spanish and return the error message translated from the Spanish catalog (error.dsn.not.found), proving Accept-Language support actually changes the rendered text rather than always returning English.",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/no-such-dsn-xyz-{{DSNUUID}}/",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["application/json"],
            "Accept-Language": ["es"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        }
    },
    "response": {
        "status": 400,
        "headers": {
            "Content-Type": ["error+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "error message is in Spanish",
            "query": "msg",
            "op": "contains",
            "value": "no existe tal nombre de origen de datos"
        }
    ]
}
