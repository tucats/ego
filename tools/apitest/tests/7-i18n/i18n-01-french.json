{
    "description": "GET a nonexistent DSN with Accept-Language: fr. The server should negotiate French and return the error message translated from the French catalog (error.dsn.not.found), proving Accept-Language support actually changes the rendered text rather than always returning English.",
    "request": {
        "method": "GET",
        "endpoint": "/dsns/no-such-dsn-xyz-{{DSNUUID}}/",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["application/json"],
            "Accept-Language": ["fr"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        }
    },
    "response": {
        "status": 400,
        "headers": {
            "Content-Type": ["error+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "error message is in French",
            "query": "msg",
            "op": "contains",
            "value": "nom de source de données inexistant"
        }
    ]
}
