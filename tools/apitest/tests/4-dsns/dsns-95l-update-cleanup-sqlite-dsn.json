{
    "description": "clean up the sqlite DSN created for the UpdateDSNHandler field-coverage tests",
    "request": {
        "method": "DELETE",
        "endpoint": "/dsns/udsn2-{{DSNUUID}}",
        "headers": {
            "Content-Type": ["application/json"],
            "Accept": ["*/*"],
            "Authorization": ["Bearer {{API_TOKEN}}"]
        }
    },
    "response": {
        "status": 200,
        "headers": {
            "Content-Type": ["application/vnd.ego.dsn+json"]
        }
    },
    "tests": [
        {
            "name": "api version",
            "query": "server.api",
            "value": "1"
        },
        {
            "name": "server id",
            "query": "server.id",
            "value": "{{SERVER_ID}}"
        }
    ],
    "tasks": [
        {
            "command": "DELETE",
            "parameters": ["{{ROOT}}/db-udsn2-{{DSNUUID}}.db"]
        },
        {
            "command": "DELETE",
            "parameters": ["{{ROOT}}/db-udsn2-{{DSNUUID}}.db-shm"]
        },
        {
            "command": "DELETE",
            "parameters": ["{{ROOT}}/db-udsn2-{{DSNUUID}}.db-wal"]
        }
    ]
}
