*.db
*.db-shm
*.db-wal
test.ego
test1.ego
go.sum
.DS_Store
ego-server.pid
user-database.json
*.crt
*.key
*.log
*.code-workspace
*.zip
*.pem

# Files used by AI tools
.claude/

# Files generated by the build. The lib.zip archive is produced by
# "go generate" from the lib directory at the root of the workspace, and is
# embedded into the executable; see internal/cli/app/library.go. It is already
# covered by the "*.zip" rule above, but is named here as well so it is
# obvious that it is a build product and not something to be committed.
internal/cli/app/lib.zip
internal/i18n/messages.go

ego
ego.exe

builds
out
__debug_bin

# Files generated from container testing
ego-container
ego-container/

# Node modules for the dashboard startup check (tools/dashboard).
# Installed on demand with "cd tools/dashboard && npm install".
node_modules/
