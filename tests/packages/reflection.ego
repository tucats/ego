
@test "packages: reflect of packages"

{
    p := reflect(strings)

    @assert p.basetype == "package"
    @assert p.type == "strings"
    @assert index(p.members, "Camel") > -1
    @assert p.native == false
}

@pass


