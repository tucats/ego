package strings

function camel(s) string {
   if len(s) == 0 {
      return ""
   }   
   if len(s) == 1 {
      return strings.upper(s)
   }

   left := strings.upper(strings.left(s, 1))
   right := strings.lower(strings.right(s, len(s)-1))

   return (left + right)
}

