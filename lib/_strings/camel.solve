package _strings

function camel(s) string {
   if len(s) == 0 {
      return ""
   }   
   if len(s) == 1 {
      return _strings.upper(s)
   }

   left := _strings.upper(_strings.left(s, 1))
   right := _strings.lower(_strings.right(s, len(s)-1))

   return (left + right)
}

