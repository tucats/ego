
func main() int {
   start := time.Now()
   count := 10

   for i := 0; i < count; i++ {
      x := math.Factor(1234567890)
   }


   finish := time.Now()
   fmt.Printf("Finished, elapsed time %s\n", finish.Sub(start).String())

   return 0
 
}

